# Hoshi 0.8 Frontend — lokale Config (kopiere nach `.env.local`, nie committen)

# Basis-URL des 0.8-Backends. Default :8090.
VITE_API_BASE=http://localhost:8090

# Token für geschützte Pfade (X-Hoshi-Token-Header). Leer = Auth-Wand greift (401).
VITE_TOKEN=
